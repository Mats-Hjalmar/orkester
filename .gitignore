# JetBrains project metadata
.idea/

# macOS
.DS_Store

# Node / pnpm
node_modules/
dist/

# bun is the script runner only; installs are pnpm. A bun.lock would drift
# from pnpm-lock.yaml and nothing reads it.
bun.lock

# Electron-vite build output
out/

# electron-builder packaged output
desktop/release/

# Expo export output
app/dist/

# create-feature workflow run records (local only)
.pipeline-runs/

# Claude Code local runtime (but track shared skills)
.claude/*
!.claude/skills/
