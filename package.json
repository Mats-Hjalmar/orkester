{
  "name": "orkester-workspace",
  "private": true,
  "packageManager": "bun@1.3.11",
  "scripts": {
    "build": "bun run --cwd packages/core build && bun run --cwd desktop build",
    "test": "bun run --cwd packages/core test",
    "typecheck": "bun run --filter @orkester/core --filter app typecheck",
    "desktop": "bun run --cwd packages/core build && bun run --cwd desktop dev",
    "desktop:install": "bun run --cwd packages/core build && bun run --cwd desktop install:mac",
    "desktop:uninstall": "bun run --cwd desktop uninstall:mac",
    "desktop:dist": "bun run --cwd packages/core build && bun run --cwd desktop dist:mac",
    "android": "bun run --cwd app android",
    "ios": "bun run --cwd app ios"
  },
  "devDependencies": {
    "@orkester/core": "workspace:*",
    "typescript": "~5.9.2",
    "tsup": "^8.5.0"
  },
  "workspaces": [
    "app",
    "desktop",
    "packages/*"
  ]
}
